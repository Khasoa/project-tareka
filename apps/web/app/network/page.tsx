import { PublicShell } from "@/components/layout/public-shell";

import { NetworkImpactView } from "./network-impact-view";

export default function NetworkImpactPage() {
  return (
    <PublicShell>
      <NetworkImpactView />
    </PublicShell>
  );
}
